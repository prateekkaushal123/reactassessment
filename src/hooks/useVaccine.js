import React from "react";
const endpoint = 'https://api.npoint.io/f2f302c08f22db42adc6';

const denominations = [2000, 500, 200, 100, 50, 10, 5, 2, 1];


const useVaccine = () => {

  /**
   * @description Takes the input value, validates it and produces the appropriate output.
   * @param {*} value 
   */
  const getVaccines = (value) => {

  };

  //[IMPORTANT] Do not change the following names.
  return {

    /**
     * 
     * For example, if the input value is covaxin, then the "result" should be:
     * [ { denomination: 'gurgaon', quantity: 50 } ]
     */
    // result,

    //If the input value is invalid, return proper error message.
    //  errorMessage,

    // totalSlotsAvailable,

};
}

export { useVaccine };